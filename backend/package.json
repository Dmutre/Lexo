{
  "name": "@lexo/backend",
  "version": "0.0.1",
  "private": true,
  "nx": {
    "targets": {
      "build": {
        "executor": "nx:run-commands",
        "options": {
          "command": "webpack-cli build",
          "args": [
            "--node-env=production"
          ],
          "cwd": "backend"
        },
        "configurations": {
          "development": {
            "args": [
              "--node-env=development"
            ]
          }
        }
      },
      "serve": {
        "continuous": true,
        "executor": "@nx/js:node",
        "defaultConfiguration": "development",
        "dependsOn": [
          "build"
        ],
        "options": {
          "buildTarget": "@lexo/backend:build",
          "runBuildTargetDependencies": false
        },
        "configurations": {
          "development": {
            "buildTarget": "@lexo/backend:build:development"
          },
          "production": {
            "buildTarget": "@lexo/backend:build:production"
          }
        }
      },
      "lint": {
        "executor": "@nx/eslint:lint",
        "options": {
          "lintFilePatterns": [
            "backend/**/*.ts"
          ]
        }
      },
      "test": {
        "options": {
          "passWithNoTests": true
        }
      },
      "migrate:make": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npx knex migrate:make",
          "cwd": "backend"
        }
      },
      "migrate:latest": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npx knex migrate:latest --env development --knexfile knexfile.js",
          "cwd": "backend"
        }
      },
      "migrate:rollback": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npx knex migrate:rollback --knexfile knexfile.js",
          "cwd": "backend"
        }
      },
      "seed:make": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npx knex seed:make",
          "cwd": "backend"
        }
      },
      "seed:run": {
        "executor": "nx:run-commands",
        "options": {
          "command": "npx knex seed:run --knexfile knexfile.js",
          "cwd": "backend"
        }
      }
    }
  },
  "dependencies": {
    "@nestjs/common": "^11.0.0",
    "@nestjs/core": "^11.0.0",
    "@nestjs/platform-express": "^11.0.0",
    "reflect-metadata": "^0.1.13",
    "rxjs": "^7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@nestjs/testing": "^11.0.0"
  }
}
